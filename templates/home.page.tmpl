{{template "base" .}}

{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h1>This is Alvin's Home Page</h1>
                <p>If u see this Page !</p>
            </div>
    </div>
{{end}}
